{
    "model_class": "Autoencoder",
    "model_hparams": {
        "input_dim": 128,
        "latent_dim": 2,
        "lib_size": 366145,
        "widths": [
            60,
            40,
            20
        ],
        "encoder": "Encoder(\n    # attributes\n    input_dim = 128\n    latent_dim = 2\n    widths = [60, 40, 20]\n    activation = relu\n    initializer = init\n)",
        "decoder": "Decoder(\n    # attributes\n    input_dim = 128\n    latent_dim = 2\n    widths = [60, 40, 20]\n    activation = relu\n    initializer = init\n)"
    },
    "optimizer_hparams": {},
    "logger_params": null,
    "enable_progress_bar": true,
    "debug": false,
    "check_val_every_n_epoch": 100,
    "seed": 42
}